name: pipeline prueba

on:
  pull_request:
    branches:
      - feature/*
jobs:
  print:
    runs-on: ubuntu-22.04   # Aqui debo definir el runner, en ese caso una maquina ubuntu en su version 22.04
    steps:
      - name: print hello world
        run: echo "Hello world :)"