name: Suma

on:
  pull_request:
    branches:
      - 'feature/**'
  schedule:
    - cron: '35 14 * * 4'  # UTC: equivale a 9:35 a.m. en Colombia

jobs:
    runs-on: ubuntu-latest
    steps:
      - name: Sumar dos números en bash
        shell: bash
        run: |
          num1=5
          num2=7
          suma=$((num1 + num2))
          echo "La suma de $num1 + $num2 es: $suma"
