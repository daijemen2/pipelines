name: Suma numeros

on:
  push:
    branches:
      - feature/*
  workflow_dispatch:

    inputs:
      Year:
        description: 'Año de la persona'
        required: true
        default: '10'
      Mes:
        description: 'Mes de la persona'
        required: true
        default: '02'

      Dia:
        description: 'Dia de la persona'
        required: true
        default: '02'
         
jobs:
  calcular-edad:
    runs-on: ubuntu-latest
    steps:
      - name: Calcular edad actual
        run: |
          echo "Fecha de nacimiento ingresada: ${{ inputs.year }}-${{ inputs.Mes }}-${{ inputs.Dia }}"