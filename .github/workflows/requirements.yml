name: Suma
'on':
  pull_request:
    branches:
      - feature/**
  schedule:
    - cron: 35 14 * * 4
jobs:
  suma:
    runs-on: ubuntu-latest
    steps:
      - name: Sumar dos números en bash
        run: |
          num1=5
          num2=7
          suma=$((num1 + num2))
          echo "La suma de $num1 + $num2 es: $suma"
